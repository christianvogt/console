import { WidgetDefinition } from '../../types';
import markdownWidgetDefinition from './markdown/definition';

const definitions: WidgetDefinition[] = [markdownWidgetDefinition];

export default definitions;
